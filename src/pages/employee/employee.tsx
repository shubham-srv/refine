import { useState } from "react";

import { IEmployee, employeeData } from "./employeeData";

import { AntdLayout, Table, List, Space } from "@pankod/refine-antd";

const Employee = () => {
  const [state, handleState] = useState(null);
  return <div>Employee</div>;
};

export default Employee;
